node_modules/
dist/
.source/
# src/ is now bundled in the repo (snapshot of free-code source + patches).
# Remove this line to unbundle if you want to fetch externally again.
*.log
.env
.env.local
*.pid
.DS_Store
/server
