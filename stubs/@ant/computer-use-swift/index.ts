/**
 * Stub for @ant/computer-use-swift
 *
 * Anthropic-internal Swift-based computer use backend.
 * Loaded lazily via require() inside a function gated on
 * process.platform === 'darwin'. Since jxproxy is never used
 * with the macOS-native Computer Use implementation, this stub
 * is only needed for build-time resolution.
 */

export type ComputerUseAPI = any;
